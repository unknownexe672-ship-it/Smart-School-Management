[build]
  base = "/"
  publish = "artifacts/school-app/dist"
  command = "pnpm install && pnpm --filter @workspace/school-app build"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
